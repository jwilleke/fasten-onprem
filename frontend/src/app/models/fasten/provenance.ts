// Re-exported from the lib model so the cross-cutting Provenance type has a single definition
// (the FastenDisplayModel base — in lib — must not depend on app/). See lib/models/fasten/provenance.ts.
export {Provenance} from '../../../lib/models/fasten/provenance';
